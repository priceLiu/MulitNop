@*add by hz full page*@
@model VendorModel
@{
    //page title
    ViewBag.Title = T("Admin.Catalog.Vendors.AddNew").Text;
}
@using (Html.BeginForm(null, null, FormMethod.Post, new { id = "vendor-form" }))
{
    <div class="section-header">
        <div class="title">
            <img src="@Url.Content("~/Administration/Content/images/ico-catalog.png")" alt="" />
            @T("Admin.Catalog.Vendors.AddNew") @Html.ActionLink("(" + T("Admin.Catalog.Vendors.BackToList") + ")", "List")
        </div>
        <div class="options">
            <input type="submit" name="save" class="t-button" value="@T("Admin.Common.Save")" />
            <input type="submit" name="save-continue" class="t-button" value="@T("Admin.Common.SaveContinue")" />
        </div>
    </div>
    @Html.Partial("_CreateOrUpdate", Model)
}